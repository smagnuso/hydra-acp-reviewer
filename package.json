{
  "name": "@hydra-acp/reviewer",
  "version": "0.0.1",
  "description": "Adversarial-but-honest code reviewer for hydra-acp \u2014 forks a session, reviews diffs, returns feedback via hydra:// link.",
  "license": "MIT",
  "type": "module",
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "main": "dist/index.js",
  "bin": {
    "hydra-acp-reviewer": "dist/index.js"
  },
  "scripts": {
    "build": "npm run lint && tsup",
    "watch": "tsup --watch",
    "start": "node dist/index.js",
    "dev": "tsup && node dist/index.js",
    "test": "node scripts/run-tests.mjs",
    "lint": "tsc -p . --noEmit",
    "prepublishOnly": "npm run build && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@hydra-acp/transformer": "^0.0.5",
    "ws": "^8.20.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/ws": "^8.18.1",
    "tsup": "^8.5.1",
    "tsx": "^4.20.0",
    "typescript": "^5.6.3"
  }
}
